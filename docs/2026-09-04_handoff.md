---
date: 2026-09-05 (E19 run, and synthesis 3.10 written)
status: disposable - delete once used
purpose: everything the human arm brings to the human-mouse consolidation.
         NOTHING NEEDS RUNNING - E16, E17, E18 and E19 were all sourced
         2026-09-04 and all four reproduce their dry runs object for object.
         See section 1.
supersedes: docs/2026-09-03_handoff_evening.md, which is KEPT but carries a
         banner. Do not act on its "RUN THIS" table - those two scripts have
         been run - and do not act on its git section; section 7 here is the
         current one.
next: the consolidation prompt has not arrived. Two new lines of work opened
         instead and both are complete - G1 (E16, the ruler check) and B4
         (E17 + E18 + E19, the DepMap functional check, its addendum, and the
         subtype check that closed the last compositional explanation). The
         consolidation is still ahead; section 4 remains what its prompt will
         need to settle.
---

# Handoff - the human arm, and B4's functional result

`main`, tree clean. **`E00`-`E19` are all written and all run.** `results/` and
`outputs/` are current as of 2026-09-04. There is nothing outstanding on the
pipeline.

---

## Resume prompt

```
Read CLAUDE.md, then docs/2026-09-04_handoff.md, then
docs/2026-09-03_human_arm_for_mouse_reconciliation.md, then
docs/2026-09-04_b4_declaration.md, docs/2026-09-04_b4_result.md,
docs/2026-09-04_b4_addendum.md and docs/2026-09-04_e19_subtype.md. The result,
the addendum and E19 are one object - read them together.

This repo is EXPLORATORY. Nothing is pre-registered. The sibling repo
myc_human_validation is a COMPLETED pre-registered study, frozen at d3ac60e,
not reopened, and its no-post-hoc rule does NOT apply here.

E00-E19 are written and run - there is nothing outstanding on the pipeline. I
source scripts in Positron; you write and edit them, you do not run them.

Two lines have closed: G1 (E16, the ruler check) and B4 (E17 + E18 + E19,
DepMap functional). B4 is the first functional evidence this arm has produced.
It runs AGAINST V1's dependency prediction - AND its addendum shows the cells
it was measured in do not carry the transcript configuration the prediction is
about, so it did not test V1. E19 then closed the last cheap explanation for
that: the configuration is NOT compositional, it holds inside Basal as well as
Luminal, and the mouse's own low-MYC slope for Bcl2l1 is flat, so the "human
sits below M*" route is closed too. All of it travels together; none of it
rescues any other part. Synthesis 3.10 is that whole arc end to end in one
place - read it before quoting any part of the DepMap result.

Neither line is the consolidation, which is still ahead. Section 3 of the
synthesis document is the human side of the comparison and section 8 is the
handover contract. Section 4 of this handoff lists the five decisions the
consolidation prompt needs to settle, each with a default.
```

---

## 1. NOTHING TO RUN

`E16`, `E17`, `E18` and `E19` were all sourced on **2026-09-04**, and all four
reproduce their pre-run dry runs exactly - `E16` in all 24 saved objects, `E17`
in all 15, `E18` in all 13 non-timestamp objects, `E19` in all 18. So every
number in `docs/2026-09-04_e16_respiratory_rulers.md`, in
`docs/2026-09-04_b4_result.md`, in `docs/2026-09-04_b4_addendum.md`, in
`docs/2026-09-04_e19_subtype.md` and in synthesis 2.3a, 3.1a, 3.7, 5.2, 5.5, 8.4
and 10 is read from a saved object.

`E09`-`E15` are unchanged since 2026-09-02 and should not be re-run.
**`scripts/check_sandboxes.R` should report every script `OK` except `E04`**,
which is the long-standing `NEEDS SOURCE`.

**One standing fragility, and it is not a to-do.** `data/raw/depmap` is a
gitignored **symlink** into `myc_human_validation`'s gitignored `data/raw/`. If
that directory is ever cleared, `E17` stops - it guards rather than skips - and
the three DepMap omics files need a **browser** to re-fetch, because the portal
is behind Cloudflare and figshare stops at 24Q4. `data/raw/depmap_README.md` is
tracked and carries the URLs, the figshare ids and the checksums.

---

## 2. WHERE TO READ

**`docs/2026-09-03_human_arm_for_mouse_reconciliation.md` is the entry point.**
It is organised around **apoptotic priming regulation by MYC and
OXPHOS**, because that is what the mouse comparison turns on.

| section | what it is | needed for the consolidation |
|---|---|---|
| **0** | the status of every number in the document | yes - it says which numbers need which re-source |
| **2** | **the data-source inventory.** Cohorts, how each axis is defined and on what scale, the covariate and why it is disjoint from both axes, the priming lists and the ratio grid, pinned catalogue SHAs, which saved object and column to read first. **2.3a is new (2026-09-04)** and is where `ox_rel` was measured and not adopted | **yes** - a mouse model can only be applied to variables whose construction is known |
| **3** | the priming analyses in the order they answer each other. **3.1 is the 12-transcript table and is the table to map a mouse model onto**; **3.1a** is that table inside Luminal and Basal; **3.10 is the whole functional line - B4, its addendum and E19 - end to end in one place, and is the section to read if the DepMap result is going anywhere near the merge** | **yes** |
| **4** | the compartment split, condensed. **4.4 is the hinge** - 11 of the 12 priming genes are on the same side of the compartment rule, so it cannot order them | yes, as context |
| **5** | **the human model.** Six labelled arrows, propositions M1-M5, what the model forbids, predictions V1-V7, and where it is weakest, ranked | **yes - this is the object to be confronted** |
| **6** | the standing negatives N1-N8 | **yes** - N2 is pre-registered and outranks everything here |
| **7** | what goes in the paper: sentences, figure slots, statistics, language rules | only if the merged model is paper-bound |
| **8** | **the handover contract.** 8.1 what the human arm cannot decide by construction; 8.2 the three claims worth taking to the mouse; **8.3 what counts as agreement and what does not**; 8.4 cross-species traps, **now eleven - trap 9 is about rulers, trap 10 about whether a functional test's system carries the configuration at all, trap 11 about flag counts not being effect sizes** | **yes - read 8.3 before comparing anything, and 8.4 trap 10 before reading any mouse functional result** |
| **9** | superseded statements - read this, not that | yes, to avoid quoting a withdrawn number |
| **10** | still open on the human side | yes |

Per-script notes: `e10_machinery_and_priming.md` (**R5e added 2026-09-03**),
`e11_prolif_adjusted.md`, `e14_curated_comparators.md`,
`e15_two_axis_gene_view.md`, `priming_interaction_tested.md`,
`paper_opening_human.md`, `e09_correlation_measures.md`,
**`2026-09-04_e16_respiratory_rulers.md`** (G1), and the B4 quartet
**`2026-09-04_b4_declaration.md`** + **`2026-09-04_b4_result.md`** +
**`2026-09-04_b4_addendum.md`** + **`2026-09-04_e19_subtype.md`**. Every one of
them reads from a saved object.

---

## 3. WHAT THE HUMAN ARM BRINGS TO THE CONSOLIDATION

Everything below already exists and is written down. Nothing needs re-deriving.

| what | where | one line |
|---|---|---|
| **Three claims to take to the mouse** | 8.2 | **H1** the dissociation (machinery ordered along OXPHOS, not MYC; conditioning separates them). **H2** the cytosolic anomaly (cytosolic apoptotic transcripts run *against* OXPHOS; no other membrane-spanning programme does). **H3** the priming configuration (`BCL2L1` up with `MCL1` down, gene by gene, not as a balance) |
| **The model itself** | 5.1-5.4 | six arrows, five propositions, and an explicit list of what the model **forbids** - the forbidden list is what makes a merge testable rather than decorative |
| **Seven predictions** | 5.5 | V1-V7, each with the measurement that would kill it. **V1** (BH3 profiling stratified by OXPHOS) is the one a mouse experiment most plausibly already addresses |
| **Eight standing negatives** | 6 | N1-N8. **N2 is pre-registered** - the functional `MYC x OXPHOS` interaction on apoptotic priming is null - and outranks anything exploratory in either arm |
| **The primary human table** | 3.1 | the 12 BCL2-family transcripts on both axes, both cohorts, with CIs. This is the table a mouse model gets mapped onto |
| **The variable inventory** | 2 | scale, covariate, cohort sizes, symbol handling, pinned SHAs |
| **The agreement rules** | 8.3 | written *before* seeing the mouse model, which is the only reason they are worth anything |
| **What the human arm cannot decide** | 8.1 | causation, protein, function, threshold, arrow (b), tissue-specificity. Six rows, each with what would settle it |

**Added 2026-09-03 and worth knowing before the merge:** sections **3.3a** and
**3.3b**. The OXPHOS row of the ratio-gain table reads `0 of 35 in both
cohorts`, but the zero is about reproducibility, not size - the largest single
gain in the whole grid is on OXPHOS (+0.090), `gain` reproduces its own ordering
across cohorts (+0.408 / +0.560), and **27 of 29 positive gains, 10 of 10 on
OXPHOS, are pairs whose two genes lean opposite ways on the axis**. The
consequence for the merge is in 8.3: a mouse ratio result needs its own `gain`
before it means anything, and a mouse ratio that gains where its components are
*concordant* would be a genuine surprise.

**B4 (E17 + E18), 2026-09-04, and it belongs in the merge because it is the
first FUNCTIONAL evidence the human arm has produced - and because of what
happened when it was checked.** Synthesis 5.2 arrow (f) - "the configuration
reaches protein and sets priming" - had no evidence attached to it at all.

**Two halves, and they must travel together.** *First*, `E17` asked DepMap
whether the M2 configuration produces the dependency V1 predicts. It does not:
**8 of 8 breast cells and 8 of 8 pan-cancer cells** point the opposite way at
n = 1,127, where 3 of 4 `MCL1` CIs exclude zero. OXPHOS-high cell lines are
marginally MORE MCL1-dependent and LESS BCL-XL-dependent. *Second*, `E18` /
`docs/2026-09-04_b4_addendum.md` asked the question `E17` had assumed:
**does the configuration exist in those cells?** It does not - **0 of 4 rulers**
in breast and 0 of 4 pan-cancer, `BCL2L1` running `+0.35` to `+0.43` with OXPHOS
in both tumour cohorts against `-0.11` to `+0.01` in CCLE, and the
`BCL2L1 - BBC3` gap flipping from positive in 8 of 8 tumour cells to negative in
8 of 8 CCLE cells.

**So B4 did not test V1, and V1 is untested rather than refuted.** The failure
is specific, not general: `MCL1`, `BAD`, `BBC3` and `BIK` keep their direction
on 4 of 4 rulers against both tumour cohorts. It is `BCL2L1` - the gene the
prediction is built on - that does not transfer. And **the cheap explanation is
already eliminated**: `E16` check 4 shows the tumour association survives purity
and leukocyte adjustment (`+0.419 -> +0.406` on n = 1,007), so it is not a
stroma or infiltrate artefact.

Three further limits, all in the result note and its addendum: the `MCL1` leg
**sits at the pan-essential floor** (`POLR2B` exceeds it on two of four rulers
in breast); the `BCL2L1` leg is the only readable one and is null at n = 51 and
n = 1,127 alike; and OXPHOS ranks 11th-12th of 18 arms, so nothing here is
respiratory-specific. **M2 is untouched** - it is about transcripts in tumours,
measured in tumours.

**What the merge takes from this is a rule, not a result** - synthesis 8.4 trap
10. If the mouse arm has a functional readout, the first thing to establish is
whether the mouse system carries its own 3.1-equivalent. A functional result in
a system that does not is the same non-test B4 turned out to be, and the check
is one partial Spearman panel.

**`E19` (2026-09-04) closed the last two ways out, and both closures are
handover items.** *First*, the configuration is **not compositional**: `BCL2L1`
holds inside `Basal` as well as `Luminal`, 4 of 4 rulers, both cohorts, **every
Basal interval clear of zero**, and ten of the twelve genes hold in both strata
(synthesis 3.1a). Since CCLE breast is basal-enriched 27 to 17 and its basal
subset gives the same flat answer as its pool, subtype is not the difference.
Purity was already closed by `E16` check 4. **The two systems are not measuring
different mixtures of the same thing - they disagree about the thing.** M2 comes
out BETTER supported: it survives stratification as well as adjustment.

*Second*, and this one is for the mouse arm directly: the argument that the
human cohort sits **below `M*`**, so the human marginal should match the mouse's
low-MYC slope, **required `bX(Bcl2l1)` to be positive. It is +0.01** - the mouse
doc's own "a flat `Bcl2l1`", read at `e348dd8`. That regime predicts about zero
and has no room for the human +0.39. **The ordering of the two guardians does
agree across species** (`Bcl2l1` above `Mcl1` in both) but is carried by `Mcl1`
falling in the mouse and `BCL2L1` rising in the human - same order, different
limb. `Bbc3`'s own `bX_wt` is in a gitignored mouse results object and is **not
readable at a pinned ref**; if the mouse arm can print that one line, the
trigger endpoint becomes readable the same way. That is the single cheapest
thing the mouse side could contribute to this.

**Also added 2026-09-04, and also a handover item:** synthesis
**2.3a** and **8.4 trap 9**. The mouse's bridge ruler `ox_rel` was built here to
its own recipe. It is **not** the same instrument as mitoPPS (0.913 / 0.885, and
their MYC entanglements still differ), and it is **not** the more separable
ruler in human - the mouse ordering reproduces on 2 of 6 cells, winning on the
MSigDB signature and losing on MYC mRNA and on the CollecTRI regulon.
**Separability from MYC turns out to be a property of the ruler-estimator pair,
not of the ruler**, so a cross-species claim that a ruler is "the separable one"
has to name the estimator in each species or it is half a statement. What does
NOT change: the 3.1 configuration survives every ruler tried and `BCL2L1` minus
`BBC3` stays positive in 8 of 8 ruler-by-cohort cells, so the explanation of
N2's null does not rest on a ruler choice.

---

## 4. WHAT THE CONSOLIDATION PROMPT WILL NEED TO SETTLE

Five decisions. Each has a recommended default, so the prompt can accept them
in a line rather than answer five questions.

1. **How the mouse model arrives.** The mouse repo is **not attached** and
   CLAUDE.md keeps it read-only. *Default: paste the model into the prompt* as
   prose or propositions. If it should instead be read from the repo, the prompt
   needs **both** the ref and the paths, because the only permitted access is
   `git -C /Users/gs/G/data/MK_myc_2022/myc_mouse show <ref>:<path>`.

2. **What the deliverable is.** *Default: a new merged-model document*, leaving
   synthesis section 5 intact as the human-only statement. The human arm has to
   stay readable on its own for the paper (section 7); a merge that edits
   section 5 in place destroys that. The alternatives are an in-place rewrite of
   section 5, or a comparison table plus a decision list with no merged model.

3. **Whether new human analysis is authorised.** A merge will almost certainly
   imply human analyses - the V-list is written for exactly that. *Default:
   documents first, then a named list of implied analyses for approval before
   any script is written.* Under Option A the author sources everything in
   Positron, so a merge that silently spawns four scripts spends their time, not
   mine.

4. **What a disagreement does.** 8.3 already says what counts as agreement,
   disagreement, and neither. What it does not say is whether the merged model
   may **weaken a human proposition M1-M5**. *Default: it may, and it must say
   which one and on what evidence* - except that **N2 outranks anything
   exploratory in either arm** and cannot be merged away.

5. **Whether the output is paper-bound.** If yes, section 7's figure slots,
   statistics and language rules apply, and each merged claim should name the
   sentence or figure it feeds. If no, the merged model can stay in model
   language and skip that. *Default: not paper-bound yet* - the merge is a
   scientific step, and the paper draft follows it.

**A sixth, already decided:** the consolidation does **not** close phase 1.
Phase 1 stays open at least until the next run, so section 10's open items
remain pending work rather than phase-2 decisions, and `docs/2026-08-31_aim.md`
stands as written. Revisit only when the author says so.

---

## 5. OPEN ON THE HUMAN SIDE, in the order I would do them

0. **A wider pan-essential floor panel** - the cheapest check left, and it
   decides how much of B4's `MCL1` reversal survives. **Sharpened by the
   addendum's task 3**: compared per primary gene rather than against the
   largest, `POLR2B` *exceeds* `MCL1` on two of four rulers in breast (1.69x,
   1.26x) and runs at 55-69% of it pan-cancer, with a CI excluding zero on two
   rulers there; `RPL3` is clean everywhere. So the `MCL1` leg is already at the
   floor on breast, and what the wider panel decides is how much of the
   **pan-cancer** reversal survives. It is NOT the CCLE matched null, which this
   result does not require. Addendum section 3; result note section 9.4.
   **Listed here because it is cheap and it bounds the only functional result
   this arm has - not because it outranks item 1**, which is still the most
   valuable next analysis.

0b. **Why CCLE and the tumours disagree about `BCL2L1`** - still open, and now
   with BOTH cheap answers ruled out: purity by `E16` check 4, **subtype by
   `E19`**. What is left is not cheap - selection history, the absence of
   stroma and of hypoxia and nutrient gradients, and what `BCL2L1` transcript
   means in a proliferating monoculture. **The informative next move is a
   different system**, not another cut of CCLE: a tumour-derived organoid panel
   or any cell system with a measured OXPHOS axis. Synthesis 10.1c.

0c. **`Bbc3`'s `bX_wt` from the mouse.** One line of the gitignored `gene_gate`
   table in `myc_mouse`'s `results/gate_model_verification.rds`. Not readable at
   a pinned ref, so this arm cannot get it - but with it, the trigger endpoint's
   low-MYC decomposition becomes readable the way the guardian's already is.
   **Ask for it in the consolidation prompt.** `docs/2026-09-04_e19_subtype.md`
   section 4.1.

1. **The CollecTRI regulon analysis.** `data/collectri_human/` is pinned,
   snapshotted and has never been used. Two questions in one: what arrow (b) is
   - MYC's own, larger-than-borrowed, completely unsorted association with these
   transcripts - and what the cytosolic half's shared correlate is. **The single
   most valuable next human analysis**, and its falsifier is already written
   (synthesis 10.1): if no regulon separates the halves beyond the mitochondrial
   ones, the phrase "different regulon" goes and "different correlate" replaces
   it.

   **`E16` gave this a second reason, 2026-09-04.** The CollecTRI regulon
   estimator is the one place `ox_rel` loses its separability advantage over
   mitoPPS and the absolute level (+0.093 / +0.137 against the incumbent, in
   both cohorts, where every signature estimator goes the other way). That whole
   column of synthesis 2.3a rests on **one** regulon estimator, and a second one
   - `COLLECTRI_MYC_STIM`, 739 genes - is sitting unscored in the same snapshot.
   Scoring it is the cheapest thing that could overturn the pattern. See item 5.
2. **A cytosolic stress programme that is not apoptotic**, as the next
   comparator for arrow (d). If a proteotoxic or integrated-stress-response
   module also runs against OXPHOS, H2's property is "cytosolic and
   stress-responsive" rather than "cytosolic and apoptotic". Do this before H2
   is leaned on hard in either arm.
3. **Fold the cognate-pairing test into `E10`** so synthesis 3.8 regenerates. It
   is the **only** number in that document with no script behind it. Small:
   filter `$priming` to `axis == "OXPHOS"` and the four selective sensitisers,
   group `gain` by cognate membership.
4. **`LumA` alone** - the homogeneous stratum, where stromal and immune
   composition vary least, and where prediction V6 should be checked from the
   other side.
5. Score `COLLECTRI_MYC_STIM` (739 genes, in the snapshot, never scored) as a
   fifth base; drop `ELLWOOD`; recompute the entanglement slope. Needs a
   pipeline re-run.
6. The ER-negative fatty-acid-oxidation reversal, untouched.
7. A stranded total-RNA dataset for `MT-ND6` and `CO1`/`CO2`.

**Out of scope until reopened:** MCbiclust / forkscale, survival, treatment,
METABRIC, DepMap, causal modelling on human data.

---

## 6. THINGS THAT WILL BITE

1. **No ortholog function anywhere, in either direction.** The reconciliation is
   a comparison of **conclusions**, not of gene lists. Tripwire:
   `grep -rnE "(mouse_to_human|human_to_mouse|ortholog[s]?)[[:space:]]*\(" scripts/`
   must stay empty.
2. **Never score SCAN-B without `scanb_pheno.rds$symbol_map`.** If a mouse model
   is projected onto SCAN-B by symbol, this breaks first and silently - coverage
   drops from 0.989 to 0.775 and Complex V loses its F1 head and c-ring.
3. **Never pool GSVA or mitoPPS values across cohorts - and a species is a
   cohort.** Compare correlations, patterns and rankings, never values.
4. **N2 outranks anything exploratory.** The sibling study is pre-registered,
   found the functional `MYC x OXPHOS` interaction on apoptotic priming null,
   and is frozen at `d3ac60e`. Nothing here overturns it and nothing may be
   written as if it did.
5. **Priming is not measurable in transcript abundance.** Write "carries a
   higher `BAD`/`MCL1` transcript ratio", never "is more primed". N3.
6. **A ratio result needs its `gain`.** In either species. 3.3, 3.3a, 3.3b.
6b. **A ruler result needs its estimator named.** In either species. Synthesis
   8.4 trap 9: `ox_rel` is the least MYC-entangled ruler on the MSigDB signature
   and the *most* on the CollecTRI regulon, in both cohorts. "The separable
   ruler" is not a property a ruler has on its own here.
6c. **`data/raw/depmap` is a SYMLINK into the closed repo, and this repo cannot
   regenerate what it points at.** `data/raw/depmap_README.md` says so and
   carries the checksums anyway. If `myc_human_validation`'s `data/raw/` is ever
   cleared, B4's input is gone from here too and the three omics files need a
   BROWSER to re-fetch - the portal is behind Cloudflare and figshare stops at
   24Q4. `E17` stops rather than skipping if the link breaks, because a broken
   symlink reads as an absent directory and script 14's idiom is to skip.
6d. **An endpoint that fails an n floor is not an endpoint that was null.**
   `E17` records every skipped fit in `$skipped` with its n and reason. The live
   case: navitoclax and venetoclax sit in a different PRISM screen from the MCL1
   compounds and cover 21-22 of the 28 lines against a floor of 25, so the only
   interpretable BCL-XL read was never fitted. Read `$skipped` before believing
   an endpoint is absent because it was null.

6e. **A FUNCTIONAL RESULT IN A SYSTEM THAT DOES NOT CARRY THE CONFIGURATION IS
   NOT A TEST OF IT.** Synthesis 8.4 trap 10, added 2026-09-04 from `E18`. B4
   measured guardian dependency against OXPHOS in DepMap, got the clean reverse
   of the prediction, and only afterwards was CCLE asked whether it carries the
   3.1 configuration at all. It does not - `BCL2L1` runs `+0.35` to `+0.43` with
   OXPHOS in both tumour cohorts and `-0.11` to `+0.01` in cell lines, and the
   `BCL2L1 - BBC3` gap flips sign in 8 of 8 cells. **Before any mouse functional
   readout is read as agreeing or disagreeing, measure the mouse's own
   3.1-equivalent.** This is NOT the same as 6-8 / trap 8 ("different
   measurement types are not a disagreement"); they stack.

6f. **A floor comparison must be per endpoint, not against the largest one.**
   B4-b passed because `vs_primary_max` compared each floor gene to `BCL2L1` at
   `+0.196`. Against `MCL1` at `-0.048`, `POLR2B` is 1.69x. The verdict stands
   as declared; the reading does not. Addendum section 3.

6g. **A FLAG COUNT IS NOT AN EFFECT SIZE.** Synthesis 8.4 trap 11, added
   2026-09-04 from `E19`. The between-subtype test is a SIGN test with no
   magnitude threshold, so it fires whenever a pooled value misses a narrow
   stratum range by any amount. Its largest excursion anywhere is **0.091** for
   ratios and **0.053** for genes, against about **0.36** for the D3/S1 case it
   was built to catch. Synthesis 3.7's "15 of 35 and 19 of 35" are correct
   counts of mostly-nothing, and 3.7 now says so. **Before comparing a mouse
   count with a human count, get the magnitude distribution behind each.**

6h. **NEVER RE-SCORE INSIDE A STRATUM.** GSVA and every z-score are
   cohort-relative, so re-scoring inside `Basal` builds a different axis and
   answers a different question. `E10` and `E19` subset the pooled scores, and
   `E19` reproduces `E10`'s 144 shared cells bit-equal as the control that it
   did not quietly re-score. If a mouse model is ever projected onto a human
   stratum, this is the first thing that breaks and it breaks silently.

7. **Option A holds.** Claude Code writes and edits the numbered scripts; the
   author sources them in Positron. Infrastructure - git, snapshots, provenance
   READMEs, docs - Claude Code may do directly.
8. **The rest of the cross-species traps are synthesis 8.4** and are not
   repeated here: different MitoCarta inventories per species, the composition
   null needing a species-native rebuild, human-specific confounds, the
   non-one-to-one `Bcl2a1` paralogues, and the rule that different measurement
   types are not a disagreement.

---

## 7. Git

`main`, tree clean, and **`origin/main` is up to date** - everything through
`d5636d9` was pushed 2026-09-05. That covers the whole of G1 (`E16`), B4
(`E17`, `E18`, `E19`) and synthesis 3.10; `git log 8a8a6d0..` is the run of work
if it ever needs reading back.

The commit that writes this line is unpushed until it is pushed, so for the live
figure run `git log --oneline origin/main..main` rather than trusting a count
written here.

`data/raw/depmap` is a gitignored symlink; `data/raw/depmap_README.md` is
tracked and is the only record of it.

`results/` and `outputs/` are gitignored and regenerable; `data/from_validation/`
is regenerable by re-copying from `myc_human_validation` at `d3ac60e`.
