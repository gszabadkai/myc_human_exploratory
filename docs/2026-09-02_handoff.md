---
date: 2026-09-02
status: disposable - delete once used
purpose: three scripts to run. E09, E10, E11 are all written and NONE has been
         sourced by the author.
supersedes: docs/2026-09-01_handoff_phase2.md (deleted)
---

# Handoff - three scripts to run, in any order

`github.com/gszabadkai/myc_human_exploratory`, `main` level with `origin`, tree
clean. `E00`-`E08` are written and run. **`E09`, `E10` and `E11` are written and
have never been sourced by you.**

`E10` and `E11` were each verified end to end against both real cohorts in a
scratchpad with the output paths redirected, so **every number in their findings
notes is a real number and your run should reproduce it exactly.** If it does
not, something moved and nothing downstream is trustworthy - say so rather than
working around it.

---

## Resume prompt

Paste into a fresh session started in `/Users/gs/code/myc_human_exploratory`:

```
Read CLAUDE.md, then docs/2026-09-02_handoff.md.

This repo is EXPLORATORY. Nothing here is pre-registered. The sibling repo
myc_human_validation is a COMPLETED pre-registered study, frozen at d3ac60e,
not reopened, and its CLAUDE.md's no-post-hoc rule does NOT apply here.

E00-E08 are run. E09, E10 and E11 are written but not yet sourced.

I source scripts in Positron; you write and edit them, you do not run them.
```

---

## 1. RUN THESE THREE

No dependencies between them. `E10` and `E11` both read `E08`'s output, not each
other's.

| script | time | writes |
|---|---|---|
| `E09_correlation_measures.R` | a few min (Kendall) | `correlation_measures.rds` + 2 figures |
| `E10_machinery_measures_and_priming.R` | ~15 s | `machinery_and_priming.rds`, 2 CSVs, **10 figures** |
| `E11_prolif_adjusted_machinery.R` | ~95 s | `prolif_adjusted_machinery.rds`, 1 CSV, **5 figures** |

### Assertions that will stop them if the inputs moved

- `E10` section 4 stops unless its Spearman values reproduce `E08`'s per-gene
  numbers exactly. **Expected: `max |difference| = 0e+00` over 44 genes.**
- `E10` section 3 stops if any of its 31 named Reactome pathways is missing from
  the installed MSigDB. **Expected: MSigDB `2026.1.Hs`, 1,839 REACTOME sets.**
- `E11` section 1 prints an overlap audit; **expected `2 / 0 / 1`** for the
  machinery (in the proliferation covariate / in the MYC reference / in the
  OXPHOS arm).
- Both stop if any gene fails to resolve to exactly one matrix row.

### Numbers to eyeball as they scroll past

| where | expect |
|---|---|
| `E10` overlap audit | in the OXPHOS arm: `CYCS`; in `FELSHER__MITOSTRIP`: `none`; in `M_b__MITOSTRIP`: 7 genes |
| `E10` predictors table | MitoCarta 0.453, CDC `is_mitochondrial` 0.453, pro-death 0.225 |
| `E10` gain summary | median gain negative on every axis and both measures |
| `E11` control | mitoribosome on adjusted MYC, z = 6.5 (SCAN-B) / 8.7 (TCGA) |
| `E11` machinery on MYC | z = -0.8 / -1.0 **raw**, i.e. already at background |
| `E11` composition null | every observed split INSIDE the grey bar |

---

## 2. What the three scripts are for, in one line each

- **`E09`** - is Spearman hiding anything? Pearson, bicor and Kendall over 220
  pairs, plus a natural-spline non-monotonicity probe. **This was the original
  stopping point and is still unread.** How to read its disagreements is in
  section 4 below.
- **`E10`** - the 44 canonical death genes re-annotated from Reactome and
  MitoCarta (open item 5.4), put on the MYC axis as well as OXPHOS, under
  Pearson as well as Spearman, plus all 39 BCL2-family priming ratios and their
  luminal/basal split. Note: `docs/2026-09-02_e10_machinery_and_priming.md`.
- **`E11`** - does OXPHOS still organise the machinery after proliferation
  correction, and does MYC? With the two controls that decide what the answer
  means. Note: `docs/2026-09-02_e11_prolif_adjusted.md`.

---

## 3. Findings, and which have been corrected

**Read the corrections, not just the headings.** Six findings have changed
materially since they were first written up; two changed yesterday and one
changed today.

### The plane - `docs/2026-08-31_phase1_atlas_findings.md`

| | claim | status |
|---|---|---|
| **F1** | the MYC-OXPHOS correlation is not proliferation | RESOLVED and strengthened. With every MitoCarta and E2F/G2M gene removed, 16/18 and 15/18 signatures still give rho >= 0.2 |
| **F2** | MYC mRNA carries no OXPHOS signal | stands |
| **F3** | the two genomes diverge | REFRAMED. mtDNA is FLAT, not suppressed. `MT-ND6` dropped as artefact |
| **F4** | gsva and zmean agree at 0.994 | stands - three instruments, not four |
| **F5** | present in every subtype, weakest in ER-negative | extended by S4 |
| **F6** | the mitoribosome beats OXPHOS | stands, and it is the control that saves E11 |
| **F7** | fatty-acid oxidation is the only negative arm | stands |

### The death axis - `docs/2026-09-01_phase1_celldeath_findings.md`

| | claim | status |
|---|---|---|
| **D0** | most of the death axis is mitochondrial content | stands - the frame for everything, and E11 P4 is its sharpest form |
| **D1** | pro-survival-skewed apoptotic transcriptome in OXPHOS-high tumours | NUMBER STANDS, INTERPRETATION DOES NOT - see S6 |
| **D2** | `TANG_FERROPTOSIS` fails its expression-matched null | stands - do not report its 0.398 |
| **D3** | BCL2 family splits by function | QUALIFIED. `BCL2` is -0.369 pooled, -0.009 in LumA |
| **D4/D5** | CICD unscoreable; product terms empty | stand |

### Phase 2 - `docs/2026-09-01_phase2_estimator_findings.md`

| | |
|---|---|
| **E1** | adjusting for proliferation and removing proliferation genes disagree in sign - **except in E11, where for the first time they agree** |
| **E2** | internal coherence measures proliferation content, not quality. **Drop `ELLWOOD`**, flag `ALFANO` |
| **E3** | `M_b` is the activated half of its regulon, and that half is the weak one |
| **E4** | four FULL signatures contain a BCL2-family gene; `M_b__MITOSTRIP` contains none of the 15 - but it does contain 7 of the 44, so E10/E11 never plot it |
| **M1** | the bicistronic control settles CO1 vs CO2: post-excision, not transcriptional |
| **M2** | the per-gene mtDNA deviation replicates at 0.888 |
| **M3** | the reproducible residual axis is PC2, not PC1, and is ~3.5% of variance |
| **S1** | BCL2 vs MYC is between-subtype. **E10 fig9 generalises this: on MYC the pooled ratio sits outside BOTH its compartments for 27/39 ratios in TCGA and 26/39 in SCAN-B** |
| **S2** | D1 is not self-overlap |
| **S3** | D1's magnitude lives in the tails |
| **S4** | the ER-negative gap is arm-specific; the mitoribosome shows none |
| **S5** | `Luminal` exceeds both LumA and LumB |
| **S6** | the canonical machinery reverses D1's direction, and localisation predicts the sign at 0.453 | **CORRECTED TODAY - see R1/P4** |

### New today - `docs/2026-09-02_e10_...md` and `..._e11_...md`

| | |
|---|---|
| **R1** | S6's 0.453 replicates exactly under MitoCarta 3.0, an independent localisation source agreeing with the curation on 43/44 genes. **But see P4: it is not specific to apoptosis.** |
| **R2** | `APAF1` is resolved. Reactome calls it intrinsic and it stays an outlier; MitoCarta does not carry it at all - the apoptosome is cytosolic - and on the localisation split it is unremarkable |
| **R2a** | Reactome's apoptosis annotation misses the mitochondrial matrix effectors entirely: 8 of the 44 are in no Reactome apoptosis pathway and they carry the highest median |
| **R3** | the split is an OXPHOS phenomenon - on MYC it collapses from 0.453 to 0.140 |
| **R4** | Pearson changes almost nothing over these 44 (largest gap 0.102) |
| **R5** | **the priming ratios mostly lose to their own component genes.** Median gain negative on every axis under both measures; only 4-6 of 39 gain in both cohorts |
| **R6** | `CYCS` is inside the OXPHOS subunits arm - E08 fig6's high end was partly self-correlation |
| **P1** | the MYC/OXPHOS contrast survives proliferation adjustment, both stripped estimators, and TCGA purity+leukocyte. Adjustment leaves OXPHOS spread alone (0.301 -> 0.313) and narrows MYC's (0.227 -> 0.191) |
| **P2** | **the control passes** - adjusted MYC still tracks the mitoribosome at z 6.5-8.7, so nothing was emptied |
| **P3** | **MYC never correlated with the machinery above background** (raw z -0.8/-1.0). The correction is not what removes it |
| **P4** | **the OXPHOS localisation split is composition, not apoptosis.** Observed 0.45-0.49 against a composition-matched null of 0.40-0.46; under a strict pool the null is 0.29-0.37 and the observed sits ~1 SD above, consistently but not separably |
| **P5** | what survives: the machinery is arranged along OXPHOS and not along MYC, neither explained by proliferation, both replicating at 0.88-0.92 across cohorts - but the OXPHOS arrangement is generic mitochondrial biology. **The residue worth chasing is the MYC side, which is BELOW what composition predicts** |

---

## 4. How to read E09 before you run it

- pearson departs from spearman while bicor sits WITH spearman -> heavy-tail
  artefact, and Spearman is the safer reading
- bicor sits WITH pearson against spearman -> magnitude carries real information
  that ranks discard
- a large **spline gain** with a modest spearman -> a threshold or saturating
  curve, which is the thing a rank correlation structurally cannot report

Verified before commit: bicor holds at 0.615 where two leverage points pull
pearson to 0.79; the spline gain returns 0.79 on a U-shaped control and **0.012**
on the headline MYC-OXPHOS pair. E10 R4 is a partial preview - over the 44
machinery genes the two measures differ by at most 0.102.

**Why Spearman is the default, for the record:** it is invariant to monotone
transforms, which is what makes a log GSVA score and a linear mitoPPS score
comparable at all. E09 is a sensitivity check on that, not a challenge to it.

---

## 5. Open, in rough priority order

1. **Run the three scripts** and read E09 in detail - still the stopping point.
2. **The 44 genes by compartment.** E10 fig9/fig10 split the 12 priming genes
   luminal/basal and found the MYC column is largely between-subtype. The same
   split has NOT been run on the 44, and P1 is the claim it would test.
3. **A sub-compartment-matched null** for P4. The current null matches MitoCarta
   membership only; the machinery's 20 are 13 MOM, 5 IMS, 2 MIM and that
   distribution is not matched. This is what would settle the ~1 SD residue.
4. **Score `COLLECTRI_MYC_STIM`** (739 genes, activation-only) as a fifth base
   in `E02`. Already in the snapshot, never scored; E3 predicts about +0.25 /
   +0.10 on OXPHOS. Cheapest test of the sharpest phase-2 finding.
5. **Drop `ELLWOOD`** from the panel and recompute F1's entanglement slope.
6. **The ER-negative fatty-acid-oxidation reversal** (-0.29 / -0.16 against ~0 in
   ER-positive) - the largest arm-level stratum effect in the study, untouched.
7. A **stranded, total-RNA (non-polyA)** dataset for `MT-ND6` and `CO1`/`CO2`.
8. `BCL2L2` is on both of the author's priming lists. It is Bcl-w and
   canonically anti-apoptotic; if the pro-side entry was meant to be `HRK` or
   `BOK`, both are already scored and the grid rebuilds in minutes. `HRK` would
   need its low-expression flag carried through.

**Still out of scope until explicitly reopened:** MCbiclust / forkscale,
survival, treatment, METABRIC, DepMap, causal or mediation modelling, and
anything that revisits the validation study's hypotheses.

---

## 6. Traps found the hard way

Phase-2 traps 1-6 are in the scripts as comments. These are the ones added
today.

1. **A SET-LEVEL STATISTIC NEEDS A COMPOSITION-MATCHED NULL, not only an
   expression-matched one.** E10 R1 was arithmetically right and interpretively
   wrong for exactly this reason: mitochondrial genes correlate with an OXPHOS
   score whatever else they do. Whenever a finding takes the form "property X
   predicts the correlation", ask what a random set sharing X scores.
2. **An OXPHOS score correlates with much of the transcriptome.** A set can
   carry a mean |rho| of 0.27 and be doing nothing a random expression-matched
   set of its size would not do. Always report the null column, not only the
   observed one.
3. **`tibble()` evaluates its arguments in sequence and each new column shadows
   an outer object of the same name.** A column named `mitocarta` beside a data
   frame named `mitocarta` made the second reference read the logical vector.
   Rename the object, not the column.
4. **`pivot_longer(names_sep = "_")` splits on EVERY underscore.** A level named
   `adj. PROLIF_DISJOINT` produced a facet labelled `NA`. Use `names_pattern`
   anchored on the prefix you actually mean.
5. **ggplot CLIPS a caption, it does not wrap it.** Hard-wrap at about 85
   characters for an 8-9 inch figure and about 125 for a 13 inch one. `paste0`
   inserts no space, so one rendered line can be split across two source
   strings to stay inside 80 columns.
6. **Re-read every caption against the rendered image.** One said "no cross
   appears" while crosses appeared, and one said the cloud "collapses into the
   shaded band" when it visibly does not. A caption that disagrees with its own
   figure is worse than no caption.
7. **A figure's mark must mean the same thing as the caption says it does.**
   Crossing the M_b overlap on a panel that plots FELSHER was the cause of 6.
   The flag is now passed into the plot function beside its own label.
8. **E08's `canonical` column is `module`**, not `cdc_module`. E10 renames it
   only inside its own `reannot`.
9. **A scratchpad dry run leaked into `results/` and `outputs/` on 2026-09-02.**
   Those artefacts were from a superseded version of E10 and have been deleted;
   `results/` now holds only the twelve files E01-E08 produced. If a numbered
   script's output appears without you having sourced it, delete it - a partial
   object from an old version is trap 1 all over again.

---

## 7. Rules that still bind

- **Option A.** Claude Code writes and edits the numbered scripts. It does not
  run them. Infrastructure - git, snapshots, provenance READMEs, `CLAUDE.md`,
  planning and result notes - it may execute directly.
- **Exploratory posture.** Nothing is pre-registered. Report structure,
  gradients and cross-cohort reproducibility; never a p-value plucked from a
  290,190-cell grid. When something looks real, write down what would falsify it
  *before* the next analysis - `E03b`, `E05b` and `E11` section 3.1 are what
  that looks like.
- **The MYC naming contract.** Every scored estimator carries `__FULL`,
  `__MITOSTRIP`, `__PROLIFSTRIP` or `__BOTHSTRIP`. There are no bare names. Use
  the `E00` constants, not string literals. `PROLIF_DISJOINT` is disjoint from
  `FELSHER__MITOSTRIP` **alone** - it shares 97 genes with the FULL panel union.
- **Never write to `myc_human_validation` or `myc_mouse`.** Read-only via
  `git -C ... show <ref>:<path>`.
- **Human only.** The ortholog tripwire is narrowed to calls:
  `grep -rnE "(mouse_to_human|human_to_mouse|ortholog[s]?)[[:space:]]*\(" scripts/`
  must return nothing.
- **Scale discipline.** GSVA wants log VST, mitoPPS wants linear DESeq2, and
  they never share an input object. Rank correlations are immune; **Pearson is
  not**, so every Pearson in E10 states its scale - log2(normalised + 1) at gene
  level against the GSVA score.
- **Never pool GSVA or mitoPPS values across cohorts.** Compare correlations and
  patterns.
- R: no `print(n = X)` after `head()`; always `dplyr::count()`; ASCII-only
  strings; every numbered script ends with an `if (FALSE) { ... }` sandbox.
- **Gene lists selected on a statistic are descriptions, not findings**, and
  must say so wherever they appear.
